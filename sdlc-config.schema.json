{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Entelligentsia/agentic-skills/forge/sdlc-config.schema.json",
  "title": "Forge SDLC Configuration",
  "description": "Project-specific configuration generated by /forge:init. Lives at .forge/config.json in the target project.",
  "type": "object",
  "required": ["version", "project", "stack", "commands", "paths"],
  "properties": {
    "version": {
      "type": "string",
      "description": "Schema version for migration support"
    },
    "project": {
      "type": "object",
      "required": ["prefix", "name"],
      "properties": {
        "prefix": {
          "type": "string",
          "description": "Short prefix for task/sprint/bug IDs (e.g. ACME)"
        },
        "name": {
          "type": "string",
          "description": "Human-readable project name"
        },
        "description": {
          "type": "string",
          "description": "One-line project description"
        }
      }
    },
    "stack": {
      "type": "object",
      "properties": {
        "languages": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Primary languages detected"
        },
        "framework": {
          "type": "string",
          "description": "Primary backend framework"
        },
        "frontendFramework": {
          "type": "string",
          "description": "Frontend framework if detected"
        },
        "database": {
          "type": "string",
          "description": "Primary database"
        },
        "taskQueue": {
          "type": "string",
          "description": "Task queue if detected (celery, sidekiq, etc.)"
        },
        "containerisation": {
          "type": "string",
          "description": "Container tooling if detected"
        }
      }
    },
    "commands": {
      "type": "object",
      "required": ["test"],
      "properties": {
        "test": { "type": "string" },
        "build": { "type": "string" },
        "lint": { "type": "string" },
        "syntaxCheck": {
          "type": "object",
          "additionalProperties": { "type": "string" }
        },
        "migrate": { "type": "string" },
        "migrateCheck": { "type": "string" }
      }
    },
    "paths": {
      "type": "object",
      "required": ["engineering", "store", "workflows", "commands", "templates", "tools"],
      "properties": {
        "engineering": { "type": "string", "default": "engineering" },
        "store": { "type": "string", "default": ".forge/store" },
        "workflows": { "type": "string", "default": ".forge/workflows" },
        "commands": { "type": "string", "default": ".claude/commands" },
        "templates": { "type": "string", "default": ".forge/templates" },
        "tools": { "type": "string", "default": "engineering/tools" }
      }
    },
    "pipeline": {
      "type": "object",
      "description": "Optional pipeline customisation",
      "properties": {
        "skipPhases": {
          "type": "array",
          "items": { "type": "string" }
        },
        "maxReviewIterations": {
          "type": "integer",
          "default": 3
        }
      }
    },
    "sprint": {
      "type": "object",
      "description": "Optional sprint execution configuration",
      "properties": {
        "execution": {
          "type": "object",
          "properties": {
            "mode": {
              "type": "string",
              "enum": ["sequential", "wave-parallel", "full-parallel"],
              "default": "sequential"
            },
            "maxConcurrentAgents": {
              "type": "integer",
              "default": 3
            }
          }
        }
      }
    }
  }
}
