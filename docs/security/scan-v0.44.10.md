# Security Scan — forge:forge — 2026-05-21

**SHA**: not recorded (source-path scan against working tree at `/home/boni/src/forge-engineering/forge/forge/`)
**Installed**: n/a (scanning source, not installed plugin cache)
**Last updated**: 2026-05-21
**Scope**: source-path
**Install path**: `/home/boni/src/forge-engineering/forge/forge/`

### Summary

306 files scanned | **0 critical** | **0 warnings** | **2 info**

Delta from v0.44.9 → v0.44.10:
- `forge/tools/manage-versions.cjs` — fix to file-copy loop inside `addSnapshot`: strip leading `.forge/` from each `relPath` before path-joining (forge#108). Pure logic fix; no new external invocations, no new regex on user-controlled data beyond the prefix normalization.
- `forge/tools/__tests__/manage-versions.test.cjs` — new test asserting the prefix-tolerance contract.
- `forge/.claude-plugin/plugin.json` — version bump.
- `forge/migrations.json` — new top entry.
- `CHANGELOG.md` — new section.
- `forge/integrity.json` — re-generated by `gen-integrity.cjs`.

No new patterns introduced. Delta is a one-line normalization inside a tool that operates only on the project's own `.forge/` tree.

### Findings

Both info findings are the same as v0.44.9 (pre-existing, false positives):

#### [INFO] `forge/commands/health.md`:173
- **Check**: B (long base64-looking blob)
- **Issue**: 64-char hex blob matches the `[A-Za-z0-9+/=]{40,}` warning pattern.
- **Excerpt**: `EXPECTED="3ec3c970dd3d7c3001f8f373bcc40556803eadd2fc2afafb14f1c232cba4cc3f"`
- **Recommendation**: False positive. SHA-256 of `verify-integrity.cjs`, required and verified each release.

#### [INFO] `forge/tools/__tests__/*` and `forge/hooks/__tests__/*`
- **Check**: A (network calls / credential paths)
- **Issue**: Test fixtures reference `https://evil.com`, `https://example.com`, JSON schema URLs, and `CLAUDE_PLUGIN_*` env vars.
- **Recommendation**: False positives. All hits inside `__tests__/` — negative-match tests validating the permissions hook blocks such calls.

### Clean Areas

- **Check A (hooks + tools, runtime code)** — zero new hits. The `manage-versions.cjs` change is pure normalization (regex strip + path join); no new exec/spawn/network/eval/credential paths.
- **Check B (markdown)** — only the `manage-versions.cjs` docstring and CHANGELOG were edited. No new prompt-injection phrases, no zero-width chars, no suspicious frontmatter.
- **Check C (permissions)** — unchanged from v0.44.9.
- **Check D (structure)** — unchanged: 306 files, ~3.0 MB, no compiled artifacts.

### Verdict

**SAFE TO USE**

Single-tool bug-fix release. The delta is restricted to `manage-versions.cjs`'s file-copy loop — a defensive normalization step that strips a known prefix before performing a path operation on internal data. No new attack surface.
