# FORGE-S02-T03: Plan

- Create `forge/schemas/feature.schema.json` with required and optional fields for Feature.
- Add `feature_id: { "type": ["string", "null"] }` to `forge/schemas/sprint.schema.json`.
- Add `feature_id: { "type": ["string", "null"] }` to `forge/schemas/task.schema.json`.
