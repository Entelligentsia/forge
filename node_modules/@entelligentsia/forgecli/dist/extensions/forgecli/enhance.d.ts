import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";
import { extractPersonaNames } from "./lib/frontmatter-parser.js";
export { extractPersonaNames };
import { checkMaterialization, type MaterializationCheck } from "./lib/manifest-checker.js";
export { checkMaterialization, type MaterializationCheck };
export type EnhancePhase = 1 | 2 | 3;
export interface ParsedArgs {
    phase: EnhancePhase;
    extra: string;
    rawPhaseFlag: string;
}
export declare function parseEnhanceArgs(rawArgs: string): ParsedArgs;
export interface ComposeKickoffOpts {
    workflowMd: string;
    personaIdentity: string;
    parsed: ParsedArgs;
    cwd: string;
    timestamp: string;
}
export declare function composeKickoff(opts: ComposeKickoffOpts): string;
export declare function defaultNow(): Date;
export declare function formatTimestamp(d: Date): string;
export interface RegisterEnhanceOptions {
    cwd?: string;
    now?: () => Date;
}
export declare function registerEnhance(pi: ExtensionAPI, options?: RegisterEnhanceOptions): void;
