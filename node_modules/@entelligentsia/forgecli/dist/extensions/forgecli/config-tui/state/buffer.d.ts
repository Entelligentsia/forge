import type { PersonaModel } from "../../config-layer.js";
import type { ConfigLayer } from "../../config-writer.js";
import type { Tier } from "../tier-meta.js";
import type { ConfigBuffer, PhaseOverride } from "./model.js";
export declare function writePersonaEntry(buffer: ConfigBuffer, layer: ConfigLayer, persona: string, entry: PersonaModel): ConfigBuffer;
export declare function deletePersonaEntry(buffer: ConfigBuffer, layer: ConfigLayer, persona: string): ConfigBuffer;
/** Fan out a single model into all persona entries for a tier.
 *  E.g. setting Heavy → anthropic:claude-opus writes architect and supervisor. */
export declare function writeTierAssignment(buffer: ConfigBuffer, layer: ConfigLayer, tier: Tier, entry: PersonaModel): ConfigBuffer;
export declare function writePhaseOverride(buffer: ConfigBuffer, pipelineName: string, phaseRole: string, override: PhaseOverride): ConfigBuffer;
export declare function clearPhaseOverride(buffer: ConfigBuffer, pipelineName: string, phaseRole: string): ConfigBuffer;
