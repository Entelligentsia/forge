import type { ToolDefinition } from "@earendil-works/pi-coding-agent";
type RunCjsFn = (toolPath: string, argv: string[], signal: AbortSignal | undefined, timeoutMs: number, projectRoot: string) => Promise<{
    stdout: string;
    stderr: string;
}>;
export declare function buildForgeArtifact(projectRoot: string, _engineeringPath: string, toolDir: string, _runCjsOverride?: RunCjsFn): ToolDefinition;
export {};
