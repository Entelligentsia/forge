import { runUpgrade } from "../extensions/forgecli/forge-update-command.js";
export interface RunUpdateOptions {
    forgeCli: string;
    fetchImpl?: typeof fetch;
    globalRootResolver?: () => Promise<string | null>;
    upgradeRunner?: (spec: string) => ReturnType<typeof runUpgrade>;
    pkgRootOverride?: string;
}
export declare function runUpdate(args: string[], opts: RunUpdateOptions): Promise<number>;
